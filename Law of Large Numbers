import numpy as np
from numpy.random import randn
n=int(input("Number of random variable you want to consider for Sample Space: "))
count=0
for i in randn(n):
    if i>=-1 and i<=1:
        count=count+1
        
ans=count/n
print(ans)
